전체 테스트를 실행하고 결과를 분석해줘.

1. `mvn test` 실행
2. 결과를 분석해서 아래 형식으로 보고:

```
🧪 테스트 결과
━━━━━━━━━━━━━━━━━━━━
✅ 성공: N개
❌ 실패: N개
⏭️ 스킵: N개
⏱️ 소요 시간: N초

[실패한 테스트가 있으면]
❌ 실패 상세:
  - 모듈명 > 테스트클래스 > 메서드명
    원인: [에러 메시지 요약]
    제안: [수정 방법]
```

3. 실패한 테스트가 있으면 수정 방안을 제시
4. 테스트 커버리지가 낮은 클래스가 있으면 알려줘

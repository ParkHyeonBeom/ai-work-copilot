apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-router-service
  namespace: workcopilot
  labels:
    app: ai-router-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-router-service
  template:
    metadata:
      labels:
        app: ai-router-service
    spec:
      containers:
        - name: ai-router-service
          image: workcopilot/ai-router-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "k8s"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_SECRET
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: openai-secret
                  key: OPENAI_API_KEY
                  optional: true
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            tcpSocket:
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 30

apiVersion: v1
kind: Service
metadata:
  name: ai-router-service
  namespace: workcopilot
spec:
  type: ClusterIP
  selector:
    app: ai-router-service
  ports:
    - port: 8083
      targetPort: 8083

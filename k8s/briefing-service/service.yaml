apiVersion: v1
kind: Service
metadata:
  name: briefing-service
  namespace: workcopilot
spec:
  type: ClusterIP
  selector:
    app: briefing-service
  ports:
    - port: 8084
      targetPort: 8084

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-service-data
  namespace: workcopilot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path

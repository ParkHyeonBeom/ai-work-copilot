apiVersion: v1
kind: Service
metadata:
  name: integration-service
  namespace: workcopilot
spec:
  type: ClusterIP
  selector:
    app: integration-service
  ports:
    - port: 8082
      targetPort: 8082
